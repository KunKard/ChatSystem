# ============================================================
#  Unity 工程 .gitignore
#  原则：能被 Unity 重新生成的，一律不入库
# ============================================================

# ---- Unity 生成目录 ----
# Library 实测 2 GB，是导入缓存，删掉重开工程会自动重建
/[Ll]ibrary/
/[Tt]emp/
/[Oo]bj/
/[Bb]uild/
/[Bb]uilds/
/[Mm]emoryCaptures/
/[Rr]ecordings/
/[Uu]ser[Ss]ettings/
/[Aa]ssets/[Aa]ssetStoreTools*
/[Aa]ssets/[Pp]lugins/[Ee]ditor/JetBrains*

# ---- Logs：只忽略 Unity 的日志，保留 Docs/ 设计文档 ----
/Logs/*.log
/Logs/*.txt
sysinfo.txt
crashlytics-build.properties
*.stackdump

# ============================================================
#  ⛔ 聊天记录原始导出：永久不入库
#
#  这里放的是从微信导出的真实聊天记录，含真实姓名、微信号（wxid）、
#  消息 ID 和完整对话正文 —— 涉及的不只是本人，还有对话的另一方。
#  本工程是公开仓库，且要录屏演示，一旦推送就无法收回
#  （git 历史里的东西删不干净，GitHub 还会留缓存和 fork）。
#
#  要用的内容一律"摘录并脱敏后"写进 Assets/ 里的对话资产，
#  原始文件只作为本地素材留在磁盘上，不参与版本管理。
#
#  ⚠️ 如果这个目录曾经被提交过，光加 .gitignore 是没用的 ——
#     已入库的文件会继续被跟踪，必须 git rm --cached 才会松开。
# ============================================================
/ChatFiles/

# ---- IDE 生成的项目文件（Unity 会按 asmdef 重新生成）----
*.csproj
*.unityproj
*.sln
*.suo
*.user
*.userprefs
*.pidb
*.booproj
*.svd
*.pdb
*.mdb
*.opendb
*.VC.db

# ---- 但 .logiccheck 是手写的验证工程，源码要入库 ----
!/.logiccheck/.logiccheck.csproj
/.logiccheck/bin/
/.logiccheck/obj/

# ---- 系统文件 ----
.DS_Store
Thumbs.db
desktop.ini

# ============================================================
#  字体：源 TTF 与派生出的 SDF 图集都不入库
#
#  ① 授权：Genshin.ttf 是米哈游专有字体（客户端提取物），本工程为公开仓库。
#     详见 Logs/Docs/Design.md 附录 A.3
#
#  ② 体积：SDF 图集实测 18 MB，属于"能被重新生成的二进制"。
#     字体图集每重新生成一次就产生一个全新的 18 MB blob 永久留在历史里，
#     实测会让 git push 直接超时失败（HTTP 502/408）。
#
#  ⚠️ 但 Genshin SDF.asset.meta 必须入库，且生成时路径不能变。
#     GUID f8f38b0ba8bf4ef4d91831e7d8b27c62 被 TMP Settings 的
#     m_defaultFontAsset（全工程默认字体）与 5 个预制体/场景引用。
#     留着 .meta 再按原路径重新生成，AssetDatabase 会复用同一 GUID，
#     上述引用全部自动恢复，无需手工重接。
#
#  换字体时按同样规则处理：新字体的 SDF 也会被下面这条通配符挡住，
#  只需保证它的 .meta 入库、且生成路径与 .meta 同级同名。
# ============================================================
/Assets/TextMesh Pro/Fonts/Genshin.ttf
/Assets/TextMesh Pro/Fonts/Genshin.ttf.meta
/Assets/TextMesh Pro/Fonts/*SDF.asset
